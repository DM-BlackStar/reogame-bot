# Reogame Bot

星环之路 (Reogame) 智能自动化机器人 - 使用 TypeScript 开发

## 功能特性

- 🤖 **自动玩游戏**: 自动建造舰船、升级建筑、研究科技
- 🌐 **API 控制**: 提供 REST API 随时查看状态和调整策略
- ⚙️ **可配置**: 通过 API 或配置文件调整游戏策略
- 📊 **状态监控**: 实时监控游戏状态和自动化统计
- 📝 **详细日志**: 记录所有操作，便于调试和分析

## 快速开始

### 安装

```bash
npm install
```

### 配置

1. 复制配置示例文件：
```bash
cp config.example.json config.json
```

2. 根据需要编辑 `config.json`：
```json
{
  "server": {
    "port": 3000,
    "host": "0.0.0.0"
  },
  "game": {
    "mainPlanetId": 你的主星球ID,
    "shipBuildBatch": 10,
    "minColonyShips": 10,
    "techTargetExpedition": 5
  },
  "automation": {
    "enabled": true,
    "intervalMs": 600000
  }
}
```

3. （可选）创建 `credentials.json` 存储敏感信息（此文件会被 .gitignore 忽略）

### 运行

```bash
# 编译
npm run build

# 启动
npm start

# 开发模式（自动重载）
npm run dev
```

## API 接口

### 状态查询

| 接口 | 方法 | 说明 |
|------|------|------|
| `/health` | GET | 健康检查 |
| `/api/status` | GET | 完整游戏状态 |
| `/api/planets` | GET | 星球列表 |
| `/api/planets/:id` | GET | 星球详情 |
| `/api/tech` | GET | 科技状态 |
| `/api/config` | GET | 获取配置 |
| `/api/logs` | GET | 获取日志 |

### 操作控制

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/action/run` | POST | 立即执行自动化 |
| `/api/action/start` | POST | 启动自动化 |
| `/api/action/stop` | POST | 停止自动化 |
| `/api/config` | POST | 更新配置 |
| `/api/action/build-ship` | POST | 建造舰船 |
| `/api/action/build-building` | POST | 升级建筑 |
| `/api/action/research` | POST | 研究科技 |

### 使用示例

```bash
# 获取游戏状态
curl http://localhost:3000/api/status

# 启动自动化
curl -X POST http://localhost:3000/api/action/start

# 立即执行一次
curl -X POST http://localhost:3000/api/action/run

# 更新舰船建造优先级
curl -X POST http://localhost:3000/api/config \
  -H "Content-Type: application/json" \
  -d '{"automation":{"shipPriority":[313,312,311,307,304]}}'
```

## 项目结构

```
reogame-bot/
├── src/
│   ├── index.ts          # 主入口
│   ├── config/
│   │   └── index.ts     # 配置加载
│   ├── game/
│   │   ├── types.ts     # 类型定义
│   │   ├── client.ts    # OGAME CLI 封装
│   │   └── automation.ts # 自动游戏逻辑
│   └── api/
│       └── server.ts     # API 服务器
├── config.example.json   # 配置示例
├── credentials.json      # 凭据（不提交）
├── package.json
├── tsconfig.json
└── .gitignore
```

## 配置说明

### game

| 配置项 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| mainPlanetId | number | 68168801 | 主星球ID |
| shipBuildBatch | number | 10 | 每次建造舰船数量 |
| minColonyShips | number | 10 | 维持最小殖民船数量 |
| techTargetExpedition | number | 5 | 探险技术目标等级 |

### automation

| 配置项 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| enabled | boolean | true | 是否启用自动化 |
| intervalMs | number | 600000 | 执行间隔（毫秒） |
| shipPriority | number[] | [313,312,...] | 舰船建造优先级 |
| buildingPriority | number[] | [100,101,...] | 建筑升级优先级 |

## 舰船 ID

| ID | 名称 |
|----|------|
| 300 | 殖民船 |
| 301 | 轻型战机 |
| 302 | 重型战机 |
| 303 | 巡洋舰 |
| 304 | 战列舰 |
| 305 | 战列巡洋舰 |
| 306 | 运输机 |
| 309 | 回收船 |
| 311 | 驱逐舰 |
| 312 | 行星轰炸机 |
| 313 | 星际战舰 |

## 建筑 ID

| ID | 名称 |
|----|------|
| 100 | 金属厂 |
| 101 | 水晶厂 |
| 102 | 重氢厂 |
| 103 | 太阳能电站 |
| 105 | 聚变反应堆 |
| 106 | 机器人工厂 |
| 108 | 造船厂 |
| 109 | 金属仓库 |
| 110 | 水晶仓库 |
| 111 | 重氢仓库 |
| 112 | 研究院 |

## 科技 ID

| ID | 名称 |
|----|------|
| 214 | 探险技术 |
| 209 | 超空间引擎 |
| 208 | 脉冲引擎 |
| 202 | 武器技术 |
| 203 | 护盾技术 |
| 204 | 装甲技术 |

## 注意事项

- 确保 `ogame` CLI 已安装并可用
- 配置文件和凭据文件已加入 .gitignore，不会提交到仓库
- 首次运行建议先测试 API 是否正常工作

## License

MIT
